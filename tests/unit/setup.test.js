/**
 * Setup test untuk memastikan Jest berfungsi dengan baik
 */

describe('Setup Test', () => {
  test('Jest is working', () => {
    expect(true).toBe(true);
  });

  test('Basic arithmetic', () => {
    expect(1 + 1).toBe(2);
  });
});
